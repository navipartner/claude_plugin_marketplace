# Plugins

### AL ID Manager
Allows the LLM to grab next AL object ID or next AL table/tableextension/enum/enumextension field ID.

### BC Dev CLI
Allows the LLM to download symbols, compile, publish apps and run tests against BC NSTs.
